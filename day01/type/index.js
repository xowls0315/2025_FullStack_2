const test1 = "10";
const test2 = 10;

// 문자 타입(String Type)
// ''(홑따옴표) ""(쌍따옴표) ``(백틱)
const a = "순대";
const a1 = "순대";
const a2 = `순대`;

// 숫자 타입(Number Type)
const b = 1;
const b1 = 123123;

// 문자타입 -> 숫자타입
// 1. Number()
const c = Number("100") + 10;
console.log(`결과 c: ${c}`);

// 불리언 타입 (Boolean)[true or false]
const d = true;
const d1 = false;
